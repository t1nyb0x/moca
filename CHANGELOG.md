# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、版番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [未リリース]

### 追加

- **身振りを割り当てられるようにした**（要件 F-15、[ADR-0019](docs/adr/0019-gestures-from-user-vrma.md)）。
  VRMA を読み込んで好きなタグ名を付けると、その名前がシステムプロンプトへ載る。
  返答に `[wave]` のように出たら、その動きをする。読み上げをしている場合は
  その音声に合わせて始まる。`[wave:0.5]` と書けば動きが小さくなる
- **診断パネルから身振りを試せる。** 割り当てごとに読み込めたかどうかが出て、
  その場で動かせる。直近の応答にタグが何度出たかも分かる。動かない原因を
  「モデルがタグを出していない」「ファイルを読めていない」「経路が繋がって
  いない」に切り分けられる（要件 F-15-11）
- **CeVIO AI へ COM で直に繋げるようにした。** 合成器の選択が VOICEVOX /
  CeVIO AI (直接) / CeVIO AI (shirataki 経由) の 3 択になった。「直接」では
  CeVIO AI 本体だけを起動しておけばよく、shirataki は要らない
  （[ADR-0018](docs/adr/0018-cevio-over-com.md)）

### 修正

- **構図が「足元は y=0」を前提にしていた。** 原点が足元にないモデルでは上下に
  ずれ、宙に浮いて見える。外接箱の下端を実測して床とするようにした
- 診断に背丈・頭の高さ・足元の高さを出すようにした。構図がずれているとき、
  モデル側の原点がずれているのかどうかを見て切り分けられる

### 変更

- CeVIO を別の PC で動かす場合は shirataki 経由を選ぶ。COM で叩けるのは同じ
  機械の上に居る場合だけなので、shirataki 経由の経路はそのまま残している

### 決めたこと

- **「モーションクリップは読まない」という 0.7 の決定は変えていない。** 素の
  moca は VRM さえあれば何も用意せずに動く。そのうえで、利用者が任意で足せる
  口だけを開けた。PMX のモーフ割り当てと違い、割り当てないと壊れるものが無い
- 身振りのタグ名は moca 側で固定しない。動きの中身を知っているのはファイルを
  選んだ本人だけであるため。キャラクターごとに違う持ち技を持たせられる
- 割り当てていないタグは従来どおり本文として表示する。すべての角括弧を飲み込む
  と、本文中の `[検索]` のような表記まで消えるため

### 既知の制限

- **動きは同梱していない。** 利用者が VRMA を用意して割り当てる
- 身振りは VRM のみ。PMX はボーン名が標準化されていないため当たらない
- クリップからは体の回転だけを使う。表情・視線・腰の移動は使わない。読み上げ中
  の口と感情の表情を moca が持ち続けるため、およびマスコット表示で窓から出ない
  ようにするため
- 身振りのあいだ、待機の揺れと呼吸はクリップが動かすボーンでは止まる
- 一度に動く身振りは一つだけ

## [0.7.0] - 2026-08-30

止まって見えないようにした。待機中もわずかに動き、感情に応じて姿勢が変わる。

### 追加

- **感情に応じた姿勢。** 喜びで顎が上がり胸が開く、悲しみでうつむき肩が落ちる、
  といった具合に体つきが変わる。感情タグの強さに応じて度合いも変わる
- **待機中の動き。** 背骨と首がゆっくり揺れ、ときおり首を傾げる。感情によって
  揺れの速さと大きさも変わる。速く小刻みなら高揚、遅く小さければ沈みに見える
- 驚きには立ち上がりの一撃を持たせた。驚きは持続する姿勢ではなく跳ねて戻るもの
  であるため
- **体の動きの切り替え。** ほかのアイドル挙動と同じく個別に入り切りできる

### 変更

- 全身の構図でモデルの頭と足が切れていたのを直した。視野 30 度では、カメラを
  身長の 1.7 倍に置くと縦に 0.911 倍しか入らない

### 修正

- 待機の揺れで腰を回しており、脚ごと振れていた。立ち姿が吊り下げられた人形の
  ように見えていた。腰は骨格の根なので、揺らすのは腰から上だけにした

### 決めたこと

- **moca は VRM と PMX に留まり、Unity 資産を直接扱わない**（[ADR-0017](docs/adr/0017-vrm-pmx-only.md)）。
  変換はアプリの外で行い、VRM へ落としてから読む
- マスコットとしての自然な振る舞い（布が正しく振る舞い、座ったり大きく動いたり
  すること）は目標に含めない。VRM の揺れ物は「垂れて押しのけられる」だけで、
  布として掛ける仕組みではない
- 1.0 の条件から PMX の実用を外した。統一規格が無く、達成できる見込みが無い

### 既知の制限

- モーションはボーンの回転による近似で、大振りな動きは作れない。座る・手を振る
  といった動作は範囲外（要件 F-14、F-15）
- PMX ではボーン名が標準化されていないため、当たる胸と背骨だけが動く
- **まばたきの抑制は暫定対応**（0.4 から持ち越し）
- 合成器（VOICEVOX 本体、CeVIO AI と shirataki）は別途起動しておく必要がある
- インストーラに署名がない

## [0.6.0] - 2026-08-30

机の上に置けるようにした。透過したウィンドウでモデルだけが立ち、掴んで
動かせる。

### 追加

- **マスコット表示。** 透過・枠なし・常時最前面へ切り替えられる。表示倍率は
  画面の高さに対する背丈の割合で決め、窓ごと拡縮する。マスコット表示中は
  ホイールでも変えられる
- **窓をモデルの形に合わせる。** 外接箱まで詰め、モデルが描かれていない
  透明な部分では背後のウィンドウを操作できる（クリックスルー）
- **掴んで動かす。** ドラッグでウィンドウごと動く。動かす対象はカメラでは
  なくモデルである
- **タスクトレイ常駐。** 左クリックで窓を前へ出し、右クリックの献立から
  表示の切り替えと終了ができる
- **マスコット表示のままの会話。** モデルを押すと吹き出しが開き、窓が右へ
  広がる。モデルの位置は動かない
- **カメラ位置を自動で覚える。** 操作が落ち着いたところで保存し、次に表示
  するとき復元する
- **感情の効き具合の設定。** 感情タグの強さに掛ける割合。声色の振れが
  大きいと感じるときに下げる

### 変更

- 感情タグの強さを声へ反映するようにした。中立の声からその感情の声へ、
  強さのぶんだけ寄せる。これまでは名前だけを見ており、`[happy:0.3]` も
  `[happy:1.0]` も同じ声だった
- 感情ごとの声の割り当てを、話者の成分を取り込んだ時点で自動で作るように
  した。空のままだと合成器側に残った値でずっと読み上げられる
- 全身の構図でモデルの頭と足が切れていたのを直した。視野 30 度では距離が
  身長の 1.7 倍では足りない

### 修正

- 成分で表せている感情に抑揚まで掛けており、二重に効いて作り物めいた声に
  なっていた。専用の成分が当たったときは抑揚を触らない
- モデルの読み込みが重なると二体が場面に並び、衣装のパーツが全部ついた姿で
  表示されることがあった

### 既知の制限

- マスコット表示にはモデルの表示が要る。モデルを外すか 3D を隠すと通常表示
  へ戻る。全面が透明になると操作できない窓が残るため
- **まばたきの抑制は暫定対応**（0.4 から持ち越し）。恒久策は表情側の目もとと
  blink を別の系統として扱うこと
- 合成器（VOICEVOX 本体、CeVIO AI と shirataki）は別途起動しておく必要がある
- PMX は実験的対応。インストーラに署名がない

## [0.5.0] - 2026-08-30

会話を続けたまま接続先を切り替えられるようにした。

### 追加

- **接続先の切り替え。** 画面上部のキャラクター選択の隣から選ぶ。会話・表情・
  モデルを作り直さないので、同じ人格のままローカル LLM と外部 API を試し
  比べられる
- **使用モデルの記録。** どのモデルが書いた返答かを吹き出しの下に出す。
  生成した時点で記録するため、接続先を切り替えても過去の返答は当時の
  モデルのまま残る
- **接続先 URL の候補。** OpenAI 互換を選ぶと Ollama / LM Studio / llama.cpp の
  待ち受け先を候補から入れられる。README にも対応表を載せた
- 設定の接続先一覧に、使用中の印と感情タグの状態を出すようにした

### 変更

- 感情タグが無効な接続先を使っている間は、その旨を画面に出すようにした。
  表情が変わらない理由が分からないのが一番困るため

### 既知の制限

- 接続先の切り替えは永続で、キャラクターの設定として保存される。「この会話
  だけ試す」はできない
- 感情タグの有効・無効は接続先ごとの設定なので、切り替えると変わりうる
- 合成器（VOICEVOX 本体、CeVIO AI と shirataki）は別途起動しておく必要がある
- PMX は実験的対応。利用者がモーフを手で割り当てないと感情表現が動かない
- インストーラに署名がない

## [0.4.0] - 2026-08-29

返答を声に出して読み上げ、その音声に合わせて口を動かすようにした。

### 追加

- **音声での読み上げ。** VOICEVOX と、[shirataki](https://github.com/t1nyb0x/shirataki)
  経由の CeVIO AI に対応した。キャラクターごとに合成器と話者を選ぶ
- **感情に応じた声。** 正規化感情から各合成器の感情成分・抑揚への割り当てを
  持てる。成分の顔ぶれはキャストごとに違うため、候補名から既定の割り当てを
  推測する。当たらない感情は声色を変えず抑揚と速さで差を出す
- **音声駆動リップシンク。** 再生位置で口形を選び、その瞬間の音量で開き
  具合を決める。文字数からの推測をやめたことで、実際の発話とずれなくなった。
  小さな声でも口が動くよう、直近の最大音量で正規化している
- **声の設定画面。** 接続を確かめて話者と感情成分を取り込み、感情ごとの
  声を調整できる

### 変更

- 返答は文が閉じた時点で合成へ回すようにした。返答の完成を待たないので
  喋り始めが早い。感情が変わる位置でも切る。一つの音声に複数の感情は
  乗せられないため
- 中断すると、まだ鳴っていない読み上げも取り消すようにした

### 既知の制限

- 合成器（VOICEVOX 本体、CeVIO AI と shirataki）は別途起動しておく必要がある
- 読み上げの失敗は会話を止めない。声が出ないだけで理由は画面に出る
- インストーラに署名がない

## [0.3.0] - 2026-08-29

PMX を読み込めるようにした。ただし表示だけの実験的対応と位置づける。

### 追加

- **PMX の表示。** モデルの読み込み、テクスチャの解決、揺れ物、立ち姿の調整
- **モーフ割り当ての編集。** PMX のモーフ名はモデルごとにばらばらなので、
  推測した割り当てを画面から直せる
- **画面側のエラーの記録。** WebView で起きた失敗もログファイルに残す。
  何のエラーも出ないまま動かない状態を減らすため

### 既知の制限

- PMX は実験的対応。統一規格が無く、視線ボーンや姿勢の自動調整はモデルごとの
  作り込みになるため追わないことにした（ADR-0015 追記）
- 音声は出ない（0.4 で対応）

## [0.2.0] - 2026-08-29

0.1 で「保存はされるのに画面から使えない」状態だったものを使えるようにした。
不具合の調査を人手に頼らずに済ませるための仕組みも入れた。

### 追加

- **会話の一覧と再開。** 過去の会話を開き直せる。削除もできる
- **モデルのドラッグ＆ドロップ。** ウィンドウへ VRM を落とすと読み込む
- **カメラ操作。** 構図プリセット（顔 / 上半身 / 全身）、カメラ位置の記憶と
  復元、背景色の変更
- **ログのファイル出力。** 日次でローテーションし 7 日ぶん残す。保存先は
  診断パネルに表示する。不具合の報告に添えてもらうため
- **GPU が使われていない場合の警告。** ソフトウェア描画に落ちていること
  自体は何のエラーも出ないため、検出して知らせる
- **履歴に使うトークン数の設定。** 接続先ごとに調整できる
- 起動のスモークテスト。実際にアプリを起動し、WebView と IPC の疎通まで
  確かめる。CI で毎回走る

### 変更

- `contextBudgetTokens` を名前どおり「履歴に使うトークン数」として扱う
  ようにした。以前は文脈長として半分に割っており、名前と食い違っていた
- GitHub Actions を最新のメジャー版へ更新した
- リリースのタグ打ちを自動化した。版番号を上げて main へ取り込めば、
  インストーラの作成まで自動で進む

### 既知の制限

- PMX は読み込めない（0.3 で対応）
- 音声は出ない（0.4 で対応）
- インストーラに署名がない

## [0.1.0] - 2026-08-29

最初のリリース。VRM を表示して LLM と会話でき、返答に応じて表情と口が動く。

### 追加

- **VRM の表示。** ファイル選択による読み込みと、前回のモデルの復元。読み込み時に腕を下ろして自然な立ち姿にする
- **モデル未設定・非表示での利用。** 3D を使わず純粋なチャットとしても成立する
- **アイドル挙動。** まばたき、視線の揺らぎ、視線追従、呼吸、髪の揺れ。個別に切り替えられる
- **チャット。** 逐次表示、中断、再生成
- **接続先の管理。** OpenAI 互換（Ollama / LM Studio / llama.cpp / OpenAI）、Anthropic、Google Gemini。接続テストとモデル一覧の取得
- **感情表現。** 応答に含まれる感情タグを解析し、発話の再生位置に同期して表情を切り替える
- **疑似リップシンク。** 音声を使わず、カナから母音の口形へ写像して口を動かす
- **キャラクタープロファイル。** 人格定義とモデル、接続先をまとめて保存・切り替え
- **API キーの安全な保管。** Windows 資格情報マネージャーへ保存し、画面側へは渡さない
- **診断パネル。** モデルの素性、感情タグの送信状態、直近の応答を確認できる

### 既知の制限

- モデルのドラッグ＆ドロップに未対応（ファイル選択のみ）
- 過去の会話を開く UI が無い（保存はされる）
- カメラの構図プリセット、背景色の変更、カメラ位置の保存が未実装
- ログはファイルへ出力されない
- PMX は読み込めない（明示的に断る）

詳細は [ロードマップ](docs/roadmap.md) を参照。

### 動作環境

- Windows 10 バージョン 1809 以降 / Windows 11 (x64)
- WebView2 Runtime（インストーラに同梱）
- VRM モデルは同梱していない。利用者が用意する
