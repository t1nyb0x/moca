# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、版番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [0.4.0] - 2026-08-29

返答を声に出して読み上げ、その音声に合わせて口を動かすようにした。

### 追加

- **音声での読み上げ。** VOICEVOX と、[shirataki](https://github.com/t1nyb0x/shirataki)
  経由の CeVIO AI に対応した。キャラクターごとに合成器と話者を選ぶ
- **感情に応じた声。** 正規化感情から各合成器の感情成分・抑揚への割り当てを
  持てる。成分の顔ぶれはキャストごとに違うため、候補名から既定の割り当てを
  推測する。当たらない感情は声色を変えず抑揚と速さで差を出す
- **音声駆動リップシンク。** 再生位置で口形を選び、その瞬間の音量で開き
  具合を決める。文字数からの推測をやめたことで、実際の発話とずれなくなった。
  小さな声でも口が動くよう、直近の最大音量で正規化している
- **声の設定画面。** 接続を確かめて話者と感情成分を取り込み、感情ごとの
  声を調整できる

### 変更

- 返答は文が閉じた時点で合成へ回すようにした。返答の完成を待たないので
  喋り始めが早い。感情が変わる位置でも切る。一つの音声に複数の感情は
  乗せられないため
- 中断すると、まだ鳴っていない読み上げも取り消すようにした

### 既知の制限

- 合成器（VOICEVOX 本体、CeVIO AI と shirataki）は別途起動しておく必要がある
- 読み上げの失敗は会話を止めない。声が出ないだけで理由は画面に出る
- インストーラに署名がない

## [0.3.0] - 2026-08-29

PMX を読み込めるようにした。ただし表示だけの実験的対応と位置づける。

### 追加

- **PMX の表示。** モデルの読み込み、テクスチャの解決、揺れ物、立ち姿の調整
- **モーフ割り当ての編集。** PMX のモーフ名はモデルごとにばらばらなので、
  推測した割り当てを画面から直せる
- **画面側のエラーの記録。** WebView で起きた失敗もログファイルに残す。
  何のエラーも出ないまま動かない状態を減らすため

### 既知の制限

- PMX は実験的対応。統一規格が無く、視線ボーンや姿勢の自動調整はモデルごとの
  作り込みになるため追わないことにした（ADR-0015 追記）
- 音声は出ない（0.4 で対応）

## [0.2.0] - 2026-08-29

0.1 で「保存はされるのに画面から使えない」状態だったものを使えるようにした。
不具合の調査を人手に頼らずに済ませるための仕組みも入れた。

### 追加

- **会話の一覧と再開。** 過去の会話を開き直せる。削除もできる
- **モデルのドラッグ＆ドロップ。** ウィンドウへ VRM を落とすと読み込む
- **カメラ操作。** 構図プリセット（顔 / 上半身 / 全身）、カメラ位置の記憶と
  復元、背景色の変更
- **ログのファイル出力。** 日次でローテーションし 7 日ぶん残す。保存先は
  診断パネルに表示する。不具合の報告に添えてもらうため
- **GPU が使われていない場合の警告。** ソフトウェア描画に落ちていること
  自体は何のエラーも出ないため、検出して知らせる
- **履歴に使うトークン数の設定。** 接続先ごとに調整できる
- 起動のスモークテスト。実際にアプリを起動し、WebView と IPC の疎通まで
  確かめる。CI で毎回走る

### 変更

- `contextBudgetTokens` を名前どおり「履歴に使うトークン数」として扱う
  ようにした。以前は文脈長として半分に割っており、名前と食い違っていた
- GitHub Actions を最新のメジャー版へ更新した
- リリースのタグ打ちを自動化した。版番号を上げて main へ取り込めば、
  インストーラの作成まで自動で進む

### 既知の制限

- PMX は読み込めない（0.3 で対応）
- 音声は出ない（0.4 で対応）
- インストーラに署名がない

## [0.1.0] - 2026-08-29

最初のリリース。VRM を表示して LLM と会話でき、返答に応じて表情と口が動く。

### 追加

- **VRM の表示。** ファイル選択による読み込みと、前回のモデルの復元。読み込み時に腕を下ろして自然な立ち姿にする
- **モデル未設定・非表示での利用。** 3D を使わず純粋なチャットとしても成立する
- **アイドル挙動。** まばたき、視線の揺らぎ、視線追従、呼吸、髪の揺れ。個別に切り替えられる
- **チャット。** 逐次表示、中断、再生成
- **接続先の管理。** OpenAI 互換（Ollama / LM Studio / llama.cpp / OpenAI）、Anthropic、Google Gemini。接続テストとモデル一覧の取得
- **感情表現。** 応答に含まれる感情タグを解析し、発話の再生位置に同期して表情を切り替える
- **疑似リップシンク。** 音声を使わず、カナから母音の口形へ写像して口を動かす
- **キャラクタープロファイル。** 人格定義とモデル、接続先をまとめて保存・切り替え
- **API キーの安全な保管。** Windows 資格情報マネージャーへ保存し、画面側へは渡さない
- **診断パネル。** モデルの素性、感情タグの送信状態、直近の応答を確認できる

### 既知の制限

- モデルのドラッグ＆ドロップに未対応（ファイル選択のみ）
- 過去の会話を開く UI が無い（保存はされる）
- カメラの構図プリセット、背景色の変更、カメラ位置の保存が未実装
- ログはファイルへ出力されない
- PMX は読み込めない（明示的に断る）

詳細は [ロードマップ](docs/roadmap.md) を参照。

### 動作環境

- Windows 10 バージョン 1809 以降 / Windows 11 (x64)
- WebView2 Runtime（インストーラに同梱）
- VRM モデルは同梱していない。利用者が用意する
